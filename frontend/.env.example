VITE_API_BASE_URL=https://localhost:8443

# Google reCAPTCHA v2 site key for the public registration form.
# The value below is Google's published "always passes" test key, safe to
# commit and use for local dev — matches the backend's app.recaptcha.enabled=false
# fallback used before real keys are provisioned. Replace with a real site key in production.
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
