spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/exhibitor_registration_dev
    username: ${DB_USERNAME:exhibitor_app}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
